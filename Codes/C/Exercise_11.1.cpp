#include <bits/stdc++.h>
#include "lcdCalculate.hpp" // the lcd calculation lib conlucded in the hpp file
using namespace std;
// For this function lcd (), the first parameter is the number of other parameters
// others are the numbers need to be calculated the lcd
int main()
{
    lcd(3,4,5,15);// '3' values in total, which are 4,5,15
    // the output would be 60
    return 0;
}

